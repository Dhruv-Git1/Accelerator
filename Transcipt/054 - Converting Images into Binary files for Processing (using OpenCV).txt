Hello all. So since everyone may not have access to MATLAB, I have written some script in Python basically using OpenCV to convert the image data into the binary format that we need for simulation as well as hardware verification. Now to use this you don't have to know anything about OpenCV, but you need to install Python 3.5 after that you need to instal OpenCV using the installer, just type pip install opencv-python, so it will install opencv in your system, mine is already installed that's why it's saying like condition already satisfied. So once you have done it, so these are the two script names are same as the script name for Math Lab, so to convert an image into binary format, this is the script, save binary image file and to view this is the script, view image binary file. Ok so you go to the directory, and just type save file, save image file and in command line itself you can give all the information. So I want to convert this, LenaColor.pff and my output name should be LenaColor.mpf Ok so you just give it like that, enter and he will convert it for you don't have to specify the resolution or anything like that Everything will be found automatically by OpenCV Now to view that image we can use this script and in this script you have to modify few things, for example the resolution you have to specify,

okay it's 512 by 523 here so keep it as such and let's say return view image binary file again then our color dot min next time and you can see the output. Now if you want to see the grayscale VITAL image, this one, LENAR-GRAY. This is after image processing. You just have to change NUM color to 1 because it's one is vital by vital image and you can say LENARGRAY.N and it will show you. The resolution is different but also you need to change here so I guess This will be helpful to those who do not have access to MATLAB. Thank you.
