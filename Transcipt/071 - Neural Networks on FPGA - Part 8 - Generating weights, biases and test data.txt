Hello, in the next few videos I will be discussing about the design automation for our neural network. Some of these automations are specific to the MNIST dataset example that we used. For most of them they are generic to generating a fully connected neural network. So let's start from something which is specific to our MNIST dataset. So here the first aim is to generate the peaks and biases that we use for generating the hardware. Now the code that you see here, 99% of them they are directly coming from this website which I showed you in the introduction neural networks and deeplearning.com so they have freely provided all the source code.

I have made a minor modification for doing something extra, I'll show you what it is. But it doesn't matter which neural network software implementation you use, our aim is to get it trained and save the weights and biases into some files so that we can use it for generating our hardware. So here, okay, this is the top file, and more details about this you can read in that website because he's explaining it in much, much detail. So here basically the idea is initially not the MNIST database. So this is that database actually.

So it is loading that database and from that database the overall data is divided into three. So on the previous tutorial I mentioned like there are 60,000 images for training and 10,000 for testing. Again, this 60,000 for training is subdivided into two. That is called a training data and the validation data. So out of 60,000, 50,000 they are used for training and 10, 000 they're used for validation because during training also you need to keep track whether the neural network is performing properly because there are other issues like all fitting.

So to avoid it during the time of training itself, you do some kind of testing. That's what we call as validation data and test data is the actual data which is used for testing And this is where you are generating your neural network so this is that main python file, Network2.py and you need to pass the number of layers and number of neurons in each layer here So here you can see 784, 3030, 10, 10. This is the one we use for hardware also. So if you are using same weights and biases, these values should match. So it's very easy if you want to remove one layer. Yeah, you can just type like this.

This makes it only like four layers including this input layer only with 30 neurons in the first and then you run in the second hidden layer and then you're on the output layer. That is one thing, and this part you need, this part you need, and this is the function used for training again what is each parameter you can read in the website basically you need to pass the training data you need to say the number of iterations use for training okay so usually if the number More of the iterations are higher, better might be the performance.

Now he is going to train it for 30 times slightly improving each time and this is like the learning rate again if you put it too small it may take a lot of time if you put too large and the performance may not be good. So again details you can read and this function actually saves the weights and biases from and this train neural network into this file. So you can just specify a file name here and he will say those weights and biases here. Now the only modification that I have done is in this, I'll share this file also.

I hope there won't be any licensing issue. Anyway, in this file, you can see towards the end, there are two functions. One is to calculate the Zigmoid, the activation function. Another one is this one, Zigmoid prime, I have just added one line because sometimes we need to use sigmoid, sometimes we need to use ReLU. So this one is from the original file 1 by 1 plus e to R of minus z that is the Z So, we have to add the derivative of what or function you are using. And this is the derivative of sigmoid. This sigmoid times 1 minus sigmoid of Z. It is the derivative of sigmoids. But for ReLU, derivative is 1 if it is positive because A equal to X so derivative will give

1 and if it is negative, derivative is 0. So just have to select the appropriate one. So once you have done that, specifying number of layers, neurons here and changing the activation function here. Now you can just go and say Python train neural network dot py and he will train the neural networks. So the files that you will need are this one, MNIST loader, MNIST package dot gz and train neural network, Network2.py, these are the four files that you need. These files are for some other purpose I will show you. Okay so now here's training so you can see this epoch 0 1 so it will go up to 30 so it will take some time and you can see the result of validation

data okay so this result is using validation data not the test data and you will see like Initially it was only 93.97, the network became 94.92, 95.04 So it might get improved as the iteration progresses So I'll come back once he finishes all 30 iterations The training is completed and you can see the final accuracy is around 96.61 on the VL dash and I-Talk This is using ReLU. And remember in our hardware implementation the accuracy was around 91%. So obviously that means there is effect due to that saturation logic, otherwise it should have been much better.

Now if you see now we have this file Weights and Bias and that is where all Weights and Biases are stored. You can see it, it is stored as like multiple arrays. So this array contains all the weights. There's an array which is specifying the sizes for each layer also and there will be another array which is specifying the passes for each one. Now next task is to generate that memory initialization Gen file from this weights and biases. That is where the next script is important, this one gen weights and biases.py and this file will generate those memory initialization file.

Only thing you have to specify is the total data width. Okay we were using 16 so that's what 16. And how many bits for presenting the integer part? Again as I mentioned if you're So, we are using Zigmo here and if the inputs are normalized no doubt you can put 1 because all inputs are posted. And we just need one bit to represent the sign 0 right assuming all inputs are normalized and they are less than 1. If input plus 1 we will need at least 2 bits but after normalization there will be usually less than 1. Anyway, now how many bits for representing the weight integer part? For that we will have to know what is the largest weight value. That part I will show in another script. There is

another script which will analyze this file and will tell you how many bits you need to use to specify the weight. Okay assume that is four bits so it is four and you just have to specify the file where these weights and biases are sitting. Okay, that is this file, weights and biases.txt 6 so you just have to specify only this much. And you also have to specify the output folder, where these old MOF files have to be stored. And this guy he also generates a C file, a.h file so that if you wish you can assign these weights and biases from the processor through our AXI-Lite interface.

So the technique used is similar to the one used in our image processing. image data you can store as a header file, same way you can store the weights and biases as a header file. And instead of giving those values at design time if you wish you can give it at runtime also from software. Okay that's it now these are some of the helper function maybe you can use it for other projects also. This function is for converting decimal to binary, you just have to tell the number you want to convert, the total data width and how many bits for representing the fraction part.

How many bits for presenting integer part? You will automatically find out from these two information. And this is the main function for generating weights and biases and there you have to specify again all these parameters. So the data will, how many bits for representing here he is taking the how many bits for representing the fractional part he's just finding from here minus integer bit and for bias also how many bits are representing fractional part and the file name which is this one. The bias integer part, you are not explicitly specifying here, bias integer width is automatically calculated using this equation, data integer width and weight integer width. Why it is that?

Because if you remember the bias is added after multiplying and adding all the input and weight. So in that product term or that sum of product term, the total number of bits representing the integer part will be the sum of integers representing weight and input. So if you just add bias to that without considering the proper representation of bias So that is why we take these two sums and the integer part representing bias will be the sum of this two. And the fractional part for bias will be still we need to keep the total data with 16 so total data with minus this integer width, so that is an important point.

That's why you don't see this parameter anywhere explicitly specified. Number of digits to represent the integer part of the bias, that is always sum of the integer width of input data and integer width of weight. So once you have this we can just go ahead and run python gen weights and bias by output I am storing it in W underscore weight, underscore bias. This slash is important to treat it as a directory and we need that directory if you don't have that he will give error like he cannot find it so let's go ahead and make that directory here W underscore B.

Okay, so let's run it and you can see. So this is how we got all those weights and biases. This is how he generated and he also generated these header files, we never used it if you wish you can use it there you can see like these are the decimal representation of these numbers okay these are like fixed point representation without that decimal point, and he is directly converting them to decimal representation. And we need to pass these values like this to our IP from the processor for it to properly work. And that's how he did it.

Another useful script is this one, GenTest.py. So if you remember for simulation, Then we had this 10,000 images in the text format. So, he is also taking MNIST dataset as input. You can see here same package is going here also and he is generating those test images in text format. Now actually there are two functions inside that one is called GenTestData one is called GenAllTestData. Gen-Test data, so if you want to generate all those 10,000 just call this function Gen-Test data but if you want a file for a particular input we can call this Gen-Test Data. For example I will show both, so here you can see I am just calling Gen-Test-Data, Data width in size and you need to give a parameter test data number.

So here, you are specifying which test image you want to use. So you can give any number between 0 to 9999. Okay? So let's try it and see it. So let's give pythongen-testdata.py. And if we come here, Inside this folder TestData because at the top here I have given the output path as TestData. So that is stored here, so you can see TestData.txt, so this is that same file that we use for simulation but in simulation there was one additional number here which basically tells like to which and VIT, VIT, WIT, WID, WID-DAM, WIDDAM, ILA, ILA-S Those who are interested you can add a Mapplotlib library over here so that you can show it as a real image also. But now I am just showing it in the next format. So you can see like this is a 28x28

image and this is 7. Let's try one more number instead of 0 let's try number 1, just rerun. Now you will see this file will be all written, but we have a new file here, Visual Data 2, and if I highlight 1, yes. Looks like 2, so this corresponds to 2 actually. And this header file again as I mentioned later when you want to test it onboard the same data that is generated It is stored in the form of a header file and this array represents that it also stores this information, is the actual number which is corresponding to this particular pattern so that in software also you can send it and when data comes back you can verify it. Same thing that we are doing in textbench and that can be done from our software. This is what this guy is doing.

Now if you want to generate all 10,000 data we just call this function, alt-test data and he will generate all 10 thousand data for you. That will be also coming in the same folder so you can see like it will always be test underscore data underscore followed by a four digit number which represents which is the S-starter and the last slide he will also write which is the number corresponding to this particular So I guess for this tutorial, this much is enough. So we have seen a couple of script files. Most of them that we used in this case, they are specific for this MNIST dataset.

But I hope the functions that are present here you can reuse for any other purposes also, Python functions. The generic one which is used for generating the neural network which we call as Zynat that I will discuss in the next tutorial. Thank you.
