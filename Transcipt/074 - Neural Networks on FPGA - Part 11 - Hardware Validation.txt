Hello all, so in the last video we have completed all the hardware designs. Finally let's test whether this neural network really works on hardware. So I'm trying a very simple case. So we already have the block design generated by our Zynq script. Now you have to make minor modifications to the block design before we run it on hardware. Okay, so you can see he already interfaced the So we used a DMA controller for transferring data from memory to our neural network. The only issue is this DMA controller, stream width will be always 16 bit.

Okay he doesn't care actually what is the interface width of our neural network so in our implementation it was only 8 bit so maybe in the new version of Zynet I can manage this would be automatically updated but as of now you have to manually change it OK, so our data width is 8. So you need to choose 8. Also check this option. Underline transfer, reason I have explained in the previous tutorial. OK, so that modification you have to do manually. And now as usual, you have to create a HDL wrapper. And after that, choose that as the top module. As of now, you can see Zynq.

This HDL is the top module. Instead of that, you choose this system as the top module and generate bitstream And after that export it to SDK, our usual business Okay so just generate bitstream and after that export to SDK So let him do it In parallel what we can do is we can generate the data for testing So yesterday for simulation we already generated those 10,000 data. So one option is to put them in the SD card and use it for testing. I am attempting a simple case using the header file that gets generated. So remember we have this gen test data script.

In the previous tutorial we used this one gen all test data to generate all 10,000. But now I am just generating a single data since we just wanted to test it. So, just choose GenTestData, data width is 8 again and you can specify which data you want to test. I'm giving TestData num 0. Now if I run python gen test data dot py as we have seen before he generates the test data. So it is here. So this I already showed you, so 0th data is 7, this is for visualization purpose. What we are actually interested in is this one, data values dot h header file. So he generates a header file and the 784 values representing this digit is actually stored as a single array.

So here also you have to make minor modification, again this data type, it will be always integer type but since we are using 8 bits interface, this int should be replaced as char type because each one of them is 8. Okay so if we are using eight char, if we are use 16 short, if we're using 32, We can put it. Now other values in between values here we will have to again do some configuration in the software so that it is automatically taken care of. Okay, so you just make it count. So that's what we are going to use this header file for testing.

So I'll come back once he finishes Bistream generation. Okay, Bistream So, the new generation is completed. Now we will look at the software. I have already exported the hardware here. Now I guess you already know all the steps how to do it. So I'm not showing it again. So that is exported and that data values dot H header file that is also imported to this project file. Again remember to change it to CAP. This is actually showing which is this number actually. So, he should detect it as 7. Okay. That is there. Now the main code, this is very similar to the code that we use for image processing because we are basically using the DMA controller for just

transferring this data to our neural network. Right? No big deal. So this is where we initialize the DMA Controller. This is where we initialized the Genre-IntroP Controller and this is where There we are connecting the intro from Xynot to the intro controller. So, we don't have to connect the DMA controller interrupt because there is no special information that DMA transfer is saying like giving. Like if he says like infinite DMA transfer, it doesn't mean anything. We can check the result only when we get interrupt from the neural network, the Zynq.

So we are using only his interrupt so this is where we are connecting that interrupt and This is where we are connecting the transporting ISR for Zynet. And we just call XC-DMA-Transer to the DMA controller, and we are transferring this data values. This guy and total number of data is 28 by 28, 784. And we are transferring from memory to device and we are just waiting for the interrupt to happen. Now this DUN signal is set by the ISR. So once he gets ISR, you can see the done signal becomes high and he reads from the status register of Zynet to clear the status register. So even if you don't read it nothing will happen.

Okay so he clears the status register and we will come out of this infinite Y-loop And now he is reading from address 8. So again remember address 8 in the AXI Lite address map was representing the output, right? So, so as I note, okay, so in Xilite wrapper, output register is 2. That means from processor address 8. So we are just reading from 8 and we are just printing detected number whatever we got and expected number So you can see detector number 7, expected number 7, so he properly detected. So if you wish, as I mentioned, you can create a header file with all this data.

You will have to slightly modify this GenTest data to add all the data to the header file. So this part writing to the header part, this one, you will have to add it to here also GenTest data. That's one way or you can take all the data generated by this guy and put it in the SD card, read from SD card SD card, and do DMX-Tranzor which I have shown in the previous tutorials. All what I really want to do is we interface a camera with ZedBoard, we really show a digit through the camera and he properly detects it. That we will do at some point of time. Unfortunately I don't have access to any monitor at this point so that we will postpone and that we will do to Net, but this one you can definitely try it. Thank you.
