Now all welcome back. So we have completed the hardware design for the system using Xilinx VDMA IP core So this time we are going to develop the software application. So I have already generated the bitstream now, I'm going to export my hardware and I will include the bitstream also and I will launch the SDK Now the software part is very similar to the software that we developed for DMA So I'm not going to write the software line by line and explain. I already have the software developed, so I will use it, but I will explain you the important points.

So we'll start a new application project. Let's call it VDMA test. Let's start a new C source and let's call it VDA, MABIC-S.C And I will just copy paste the code here. Ok so at the top we have to include this header, XIVDMA, that is where the driver for XIVD MA is sitting. We also need the header for the interrupt controller because we are going to operate this on interrupt also. So remember, VDMA is going to send us an interrupt once he finishes sending one entire frame. At the top again I have declared the size of the frame, so 180 and 1920, and this is the frame size.

So frame size it is declared in number of bytes. So each pixel is 3 bytes for us, so that's why frame size is H-size time V-size times 3, so this is our interrupt controller. This is the buffer in the DDA where the data will be stored. So this is declared as static array this time I'm not using MLK instead I just declare it as a static array and the size of the array is the frame size so this is effectively the video frame in our DDA memory. Now okay so this part VDMA, VDMA-DMA, UART, UART-DMA-VIDMA, UH-DMA.H header file, so the size of the frame that you are going to configure inside this structure.

So the vertical size you specify how many pixels, horizontal size you have to specify in number of bytes. That's why it should be H size times 3 again because our pixels it's RGB bytes per pixel. Frame delay, circular buffer, enable synchronization, these are all default state. And here is the config function like before we have initialize function and config function. And here what I'm doing is you need to store the starting address of your video buffers in this structure. For that there is an array inside the structure called frame store start array.

So this loop will be used for initializing the starting addresses. Remember in our hardware design when we generated the VDMA, we specified like we have only one frame buffer. So these four loops will be running only once in that case but if you configure more than In our case it is 1, but if you have more than one this for loop will initialize all those addresses here. So the starting address of the first frame is the starting address. So, we have to pass the pointer to the VDMA and the pointer to a structure where you have configured all this information.

And this idea also makes sense. Okay so this is a constant which is specifying the direction of the DMA operation. So remember previously we had memory to DMA, DMA to memory for XIDMA controller. DMA read, XSCTDMA write, to specify the directory, whether it is read or write. In our case it is read because we are reading from DDA so they call it the read operation. After that we are configuring the setup that is written as a separate function set up in Trump system but the idea is exactly same we are passing a pointer of DMA controller as well as the IRT number from X-Pronometer. So if you go to this function here, you will see it looks exactly same as before. We are initializing the IntrepController here and we are connecting

that IntrepController with the IntrepHandler. Now what is here? This is the Intrep handler, X-ACSI, VDMA, readIntrep handler and this is a predefined function. So the IntrepService Interop service routine is not written by you. That interop service routine is directly provided by Xilinx, so that is the function here. What they do is they will automatically acknowledge the interrupt, so they will clear that interrupt with everything will be automatically done by this interrupt service routine, so you don't have to worry about it. Now there are some additional features there which we'll see soon. So this is the So this is the Intrep Service Routine, you didn't write it, you just connected that Intrep Service Routine with the IRQ number.

This one, this is where the IRQ Number is coming and the instance point. The Intrep Controller. Then you enable the Intrep and these are all standard. Okay so this is an addition thing. Again this function, XXC-VDMA set callback is again provided by Xilinx. So what happens is whenever an interrupt comes, this interrupt service routine is automatically called and this interrupt service routines can call another function, another user function so if you want to do something extra when an interrupt comes other than acknowledging it and clearing the interrupt with things like that you can write separately and you can interface that function with that Intr

using this function, that callback. You can see this is the syntax, again pointed to DMA controller and this is the pointer again, DMA controller, callback actually, and the direction and these are the two different functions that we are looking for. One function is called read call back, and one function is called read call back error. Again there are two parameters here. This is basically saying this function will be called if there is an interrupt cause of an error. And this function will called whenever we say read completion happens which is the normal operation.

So if the read operation is completed, interrupt will come, Interop service routine will be called, he will run automatically and he will call your function if you want that to happen. So here you can see that function here and currently that function does nothing but just print like read call back function is called just to show you like function is called whenever an intro comes. So this is not actually ISR But this function is called by the ISR, ISR is provided by Xilinx. So that's it. So we enabled the interrupt and this loop basically is to use the vertical stripes RGB stripes in the buffer. Okay so you can look at the code and you can easily understand so for and vertical, then we take horizontal, then J. There are three cases.

SoC, Vivado, VDMA, UART, ILA, XSCT, SD card, UART. You need to flash the cache for this data to get stored in the DDR And this is a function which is used for starting the DMA Now here as I mentioned before, once you start the DMA You can start and forget about it So he will run in a loop He will transfer the first frame And he will give you an interrupt If there is another frame, he will start And in the next frame, view the interrupts and so on and so forth Since we have only one frame He'll start that frame After that he will give interrupt, immediately he will again start sending the same frame again.

So the starting address of the frame is like constant. So he keeps on sending the same frame which is our buffer, that's it and we remain in an infillipro. So we are remaining in an infilliproot, this guy is upon sending this frame back to back in a loop and interrupt might be coming and whenever interrupt comes we should be seeing this function. And on the screen since we stored these three stripes of colors in our memory, on the screen we should be seeing the RGB stripes. So that's it so let's go ahead and you can choose Run Configuration and let's configure it standalone application. Since I have bitstream here I can program also from here and update and let's configure the Paratom also, so

FIDIL port 115-200. So now let's go ahead and program and run our software, so run configuration we can just choose run. So in Paratom you can keep on seeing read call back function called yeah because after sending each frame it's going to in this. Okay, so that's why it is going to come back to back. Now on the monitor you can see the output. So similar to our hardware implementation same output is coming. So our software driver is working fine. I wanted to add few more things here one thing is if you look at our frame buffer here you can It is declared as a global variable outside all the functions.

So everything works. Suppose you declare it as a local variable, suppose you put it inside the main, now it is a local variable and there won't be any compilation error because this variable is not used anywhere other than the main function. Now if I program my FPGA and run the software at T, OK, he is running, but you will see like no print is coming here and the screen is blank. I can tell you that means your software is not working. So why is that? It is because this memory allocation failed. Remember during our image processing time we did dynamic allocation and that time if you remember I increased the heap size because the system is allocating dynamic memory from heap.

Now if you have functions, so the variables in the function that is allocated from the stack memory and now you have a very large variable which has to be allocated from the stack and by default stack size is only hex 2000 and that's not enough for this array. So in this case you need to increase the stack size. So remember you need to increase the heap size if you are taking lot of memory dynamically You need to increase stack size if you are using lot of memory via variable inside a function. If it is a global variable that is allocated separately, okay? So really understand it.

You need to understand the format of the ELF file. So let's see how much memory you need. This is 1080p, 1080 by 1920 times 3. This is the size. It hacks this much. 5EEC. We need to get more than that in the stack. Let's give 60 followed by 40. We need to, yeah, at least 2000 let's add this. Okay, should be enough. Save it and run again. And this time it's working on the screen also it is working. So keep that in mind. Another simple application that you can develop, okay, let me show you. For that we can make advantage of this callback function.

So remember this function is called whenever an interrupt is received, whenever he finishes transmitting one frame. So what I can do is I can just make my screen to blink in black and white right? So let me do one thing so let me remove initialization for RGB even if you have it doesn't make any difference and I need to make all pixels white and keep it like that for a second, okay? And after that keep all cells as black and keep it for one second. So you feel like your screen is blinking in black and white. So let's do something like that. So I need to use the same buffer in that function also so let me make it back to global and here, okay so let's say like I need to make everything zero. So for that you can use ForLook

or there is a better function called memset function memory set if you want to initialize memory with some constant you can use this function so this is the syntax memset okay address of buffer, starting address, OK. And what data you want to initialize? So X0, 0. And how much data? So how much data, frame size. That might be better. So frame size. We need to blink alternatively. So let's have a variable. Starting it. I equal to 0. So if A0, I will do this and I will make I1. And next time I will make all cells one.

So if you make RGB 1, you will get white. If you make RGB 0, you'll get it. So now the problem with this is if you do like that, we are getting interrupt after each frame. So after each frame, it will be becoming back and right. So our frequency is 60 Hz, so if this happens at a rate of 60 Hz you won't feel that blinking effect. You may constantly see why, because of our system's efficiency. So let's put some sleep here, sleep 1 and so it is going to happen. I already have sleep touch, remember to add that. What is going to happen is you will get interrupt, okay?

After transmitting one frame, you will come here and you will make it backward-wide. After that, you will make a delay of 1 second. So any interrupt which is coming within this 1 second will be ignored, okay? So that's what is going to happened. But in my controller he will still work, okay? He is always working constantly but there won't be anymore interrupt because this place I am sleeping for 1 second, OK? Just for a demonstration. So let's save it and let me run once again. So you could see like it is blinking but not all pixels are changing there are pixels in white and white SoC, Vivado, VDMA, UART, XSCT, SD card, ILA, ILA.

We can exploit this function, decide what should happen once a frame is successfully transmitted. Okay so that's for this tutorial I'll be sharing all the code through it. Please try yourself. Thank you bye-bye.
